apiVersion: v1
kind: Service
metadata:
  name: event-processor-public
  namespace: kafka-dev
spec:
  type: LoadBalancer
  ports:
  - name: webui
    port: 8081
    targetPort: 8081
  selector:
    app: event-processor
    component: jobmanager